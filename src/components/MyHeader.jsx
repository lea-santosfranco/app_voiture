import React from 'react'

const Myheader = ({myStyle, children}) => {<h1 style={{color: myStyle}}> {{children}}</h1>
//   return ( on peut virer le return car on a qu'un seul éléménent à retourner
//   )
}

export default Myheader
